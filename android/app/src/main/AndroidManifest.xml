<?xml version="1.0" encoding="utf-8"?>
<!--
  No permissions, deliberately. The app carries the whole dashboard inside
  it and reads nothing from the network, the contacts, the location or the
  storage. Without INTERNET it cannot phone home even by mistake. Calling a
  station and opening a station's own web page both hand off to the phone's
  dialler and browser, which need no permission of ours.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="false"
        android:theme="@style/Theme.LocalityCrimeMap">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
