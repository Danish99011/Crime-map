// Top-level build file. The Android Gradle Plugin comes from maven.google.com;
// the SDK platform and build tools it needs come from dl.google.com, which
// Android Studio installs on first sync.
plugins {
    id("com.android.application") version "8.7.3" apply false
    id("org.jetbrains.kotlin.android") version "2.0.21" apply false
}
